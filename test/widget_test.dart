import'package:flutter/material.dart

import 'app.dart';
void main(){
  runApp(const MyApp());
}

import 'package:flutter/material.dart';

class HomePage extends StatelessWidget {
  const HomePage extends({Key? key: super})key: key}:

@override
widget build(BuildContext context) {
  return Scaffold(
      appBar:AppBar(

  }